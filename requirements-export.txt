# Optional export dependencies for scripts/export_model.py
# Install on the Pi (or export machine): pip install -r requirements-export.txt
#
# NCNN is recommended on Raspberry Pi ARM64. OpenVINO is optional (Intel-tuned).

ncnn
pnnx
onnx>=1.12.0,<2.0.0
openvino>=2024.0.0
